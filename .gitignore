# dependencies
/node_modules
/mcp-server/node_modules
/worker/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# mcp-server build
/mcp-server/dist/

# worker build
/worker/dist/

# superpowers sdd scratch
/.superpowers/

# Database snapshots taken before migrations — never commit, they hold every task
backups/

# Playwright
/test-results/
/playwright-report/
/blob-report/

# Per-machine permission state, and any backup of it. The exact filename is covered by a
# global ignore, which does not catch variants — one such copy reached a commit.
.claude/settings.local.json*
