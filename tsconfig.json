{
  "compilerOptions": {
    "target": "ES2017",
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    // `scripts/*.ts` are run by `node file.ts` (Node 26 strips types), and Node requires the
    // real extension on a relative import while bundler resolution forbids it. Valid only
    // alongside `noEmit`, which is set below — nothing is emitted from these paths (BP-599).
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    // The BP-409 proxied e2e server's own distDir. Committed rather than left for `next dev` to
    // add on first run: gitignored and empty on a fresh checkout, so the glob matches nothing —
    // but leaving it out means every local run of that server dirties this file the same way.
    ".next-proxied/types/**/*.ts",
    ".next-proxied/dev/types/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "mcp-server",
    "worker"
  ]
}
