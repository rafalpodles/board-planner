# BP-412 / e2e speed — stan na 2026-08-24 22:56

Commity na bp-412/e2e-board-structure: 8359e43 (seedowana sesja + bcrypt), 3b0d45c (6 grup + matrix CI).
Nic nie wypchnięte. PR nie istnieje.

## run3 (pełny suite lokalnie, po zmianie sesji): 7.4 min, 301 pass / 10 fail / 1 skip

Wynik surowy: .run3.json

### Do naprawy — 10 padniętych, obie przyczyny znane

1. **sessions-and-auth.spec.ts (8 testów)** — "expected exactly one session for this account".
   seed() wstawia teraz wiersz sesji dla admina i membera, a ten spec liczy sesje.
   Fix: ten spec musi startować bez zaseedowanych sesji — opcja w seed() (np. `seed({ sessions: false })`)
   albo skasowanie tych dwóch wierszy w jego beforeEach. To jest DOKŁADNIE ryzyko, które było przewidziane.

2. **instance-settings.spec.ts (1 test)** — czeka na przycisk `E2E Admin`; nowy signIn nie nawiguje.
   Fix: dodać page.goto przed asercją. Sprawdzić też email-on-account (signInAsAdmin = arriveSignedIn bez goto).

### Potem
- BP-412: e2e/project-settings.spec.ts (wchłania settings-save.spec.ts), podmiana w e2e/groups.ts.
- Mutacja nowych testów.
- gh api user -q .login == rafalpodles przed każdą operacją na GitHubie. Bez "merge" od rpo nie mergować.
